import { ImgWithFallbackDirective } from './img-with-fallback.directive';

describe('ImgWithFallbackDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgWithFallbackDirective();
    expect(directive).toBeTruthy();
  });
});
